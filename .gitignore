# large regenerable audio sources (transcript inputs)
*.wav
_audio/

# Big DB blobs live in the `kg-data/` submodule (private, Git LFS), never here

# Session handoff notes — local only, never committed (public repo)
handoffs/

# Brand-asset working files (claude.ai/design prompts, raw design HTML, drafts) — local only, never public
.scratch/site-design/_brand-work/

# Go-to-market research + campaign calendar — local only, never public
.scratch/launch-gtm/


# CommandCode delegation tally — local only
.commandcode-delegate
artifacts

# docs/ is allow-listed: only these three ship. Everything else under docs/ is
# local vendored reference material and stays out of the public repo.
docs/*
!docs/adr/
!docs/agents/
!docs/supporter/
